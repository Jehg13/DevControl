{"id":"pattern-001","problem":"Create products without exposing concrete construction.","code":"class ReportFactory:\n    def create(self, kind):\n        if kind == 'pdf':\n            return PdfReport()\n        return HtmlReport()","expected_patterns":["factory"]}
{"id":"pattern-002","problem":"Add caching without changing the client interface.","code":"class CacheProxy:\n    def __init__(self, service):\n        self.service = service\n        self.cache = {}\n    def get(self, key):\n        if key not in self.cache:\n            self.cache[key] = self.service.get(key)\n        return self.cache[key]","expected_patterns":["proxy","dependency_injection"]}
{"id":"pattern-003","problem":"Select interchangeable pricing policies.","code":"class Checkout:\n    def __init__(self, strategy):\n        self.strategy = strategy\n    def total(self, cart):\n        return self.strategy.calculate(cart)","expected_patterns":["strategy","dependency_injection"]}
{"id":"pattern-004","problem":"Expose a cohesive boundary over multiple subsystems.","code":"class ApplicationFacade:\n    def run(self):\n        self.loader.load()\n        self.validator.validate()\n        self.publisher.publish()","expected_patterns":["facade"]}
