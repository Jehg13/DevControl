{"id":"algo-001","problem":"Detect whether a list contains duplicates.","code":"seen = set()\nfor item in values:\n    if item in seen:\n        return True\n    seen.add(item)\nreturn False","expected_concepts":["sets","hash_tables","searching"]}
{"id":"algo-002","problem":"Process tasks in arrival order.","code":"from collections import deque\nqueue = deque(tasks)\nwhile queue:\n    task = queue.popleft()\n    process(task)","expected_concepts":["queues"]}
{"id":"algo-003","problem":"Compute repeated subproblems once.","code":"from functools import lru_cache\n@lru_cache\ndef fib(n):\n    if n < 2:\n        return n\n    return fib(n - 1) + fib(n - 2)","expected_concepts":["recursion","dynamic_programming"]}
{"id":"algo-004","problem":"Explore all candidate combinations and undo choices.","code":"def search(path):\n    if complete(path):\n        return path\n    for choice in choices(path):\n        path.append(choice)\n        result = search(path)\n        path.pop()\n        if result:\n            return result","expected_concepts":["recursion","backtracking","stacks"]}
