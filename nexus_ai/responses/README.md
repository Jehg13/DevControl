# Nexus response generation

This layer renders verified facts, inferences, proposals, plans, pending
actions, execution failures, and missing information. A tool is considered
executed only when Laravel returns `ok: true`; Python never promotes a
proposal or a pending request to an execution result.
